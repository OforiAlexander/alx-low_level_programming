#include "main.h"

/**
 * print_alphabet - trying our hands on the c functions and nested loops
 *
 * Return: Always 0 (success)
 *
 */
void print_alphabet(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		_putchar(a);
	_putchar('\n');
}
