#include "main.h"
/**
 * mul - this  multiplies two integers a and b
 *@a: checks for first integer
 *@b: checks for second integer
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	int r;

r = a * b;
return (r);
}
