#include <stdio.h>
/**
 * main - This task requires that we loop over alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void) {
    for (char a = 'a'; a <= 'z'; a++) {
        putchar(a);
    }
    putchar('\n');
    return 0;
}
